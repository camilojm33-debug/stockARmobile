# Acceso al Controlador Global de Precios

El controlador global está disponible en `/precios/` y requiere permisos administrativos.
El menú y el atajo visual se inyectan desde el `offline-manager.js` global para evitar modificar el layout base de la aplicación.
